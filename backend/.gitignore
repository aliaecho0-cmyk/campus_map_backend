# 运行 run-migration.js 生成的 SQLite 数据库文件
database.db
database.db-*

# Node.js
node_modules/

# 日志与系统文件
*.log
.DS_Store

# 工具本地目录（.claude/ 匹配任意层级的同名目录；docs/.claude/ 例外，纳入版本控制）
.claude/
!docs/.claude/

# 本地环境变量（含 JWT 密钥，严禁入库）；.env.example 示例文件允许提交
.env
