# 2026-09-10 工作记录（claude-memory）

承接 2026-09-09（已完成 db/repository/service/middleware/routes 分层）。今日做的是**入口、运行与环境、以及两处修正**。

## 1. 入口与运行（app.js / server.js / .env）
- 新增 `src/app.js`：`createApp()` —— 全局 `express.json()` + 简单请求日志 → 挂载 5 个 router（`/api/auth`→auth、`/api/events`→booth、`/api/me`→reward、`/api/me`→claim、`/api/staff`→staff）→ **末尾**注册 `errorHandler`。
- 新增 `src/server.js`：`PORT = Number(process.env.PORT) || 3000`，启动并打印 `Server running on http://localhost:3000`。
- `package.json` scripts 用 Node 内置 `--env-file` 读 `.env`（v24，未装 dotenv）：
  - `start`: `node --env-file=.env src/server.js`
  - `dev`: `node --env-file=.env --watch src/server.js`
- 创建 `backend/.env`（JWT_SECRET 为 `crypto.randomBytes(32)` 生成的 hex、STAFF_CODE=staff2026、PORT=3000、NODE_ENV=development），并加入 `.gitignore`。
- 实跑验证：`node --env-file=.env src/server.js` 后学生登录 POST /api/auth/student 返回 **200**，含 token / user.id / eventEndAt。

## 2. device_id 更改为 8 位随机段
- 正则要求 `dev_<时间戳>_<8位字符>`，但文档示例用了 6 位。
- 已将 `docs/api.md`（4 处）与 `docs/BACKEND_PLAN.md`（3 处）里的 `dev_1726735999_a8f3k2` 全部改为 8 位 **`dev_1726735999_a8f3k2XY`**。
- 仓库当前无已提交的测试脚本需要改（临时冒烟脚本跑完即删，且本身用的就是 8 位）。

## 3. 迁移时间戳修正（end_at）
- `migrations/initial_schema.sql` 种子 `end_at` 逐步改到 **`1789833599`** = `2026-09-19T15:59:59.000Z` = **北京 23:59:59**，对齐 BACKEND_PLAN §5.2 的统一失效点。
- 过程注意：`1789790399`→（先试过 1789819199=11:59:59Z/北京19:59:59，后确认）→`1789833599`。
- `database.db` 已删库重跑 `node run-migration.js` 重建并验证 `events.end_at=1789833599`。
- 因原地改种子，全新环境迁构建即得新值；已迁移库需删库重跑或写 002 增量迁移。

## 4. .env.example（可提交）
- 新增 `backend/.env.example`：JWT_SECRET/STAFF_CODE/PORT/NODE_ENV 占位。
- `.gitignore` 原为 `.env` + `.env.*`（会连 `.env.example` 一起忽略），已改为**只忽略 `.env`**。
- 验证 `git check-ignore`：`.env` 忽略 ✓，`.env.example` 未忽略（可提交）✓。

## 5. 冒烟测试脚本（test-smoke.js）
- 新增项目根目录 `test-smoke.js`（ESM + Node 内置 `fetch`），聚焦**业务逻辑正确性**；内网活动、无密码登录，故不覆盖暴力破解/限流/XSS/CSRF 等安全项。
- 共 6 组 19 步：
  - 完整流程：学生登录 → 浏览 5 摊解锁 knowitall → 查奖励 → 取码 → 工作人员登录 → 核销 → 复查已核销。
  - 幂等性：重复浏览 `uniqueBoothCount` 不变；重复取码返回同一码；重复核销 → 409 `CLAIM_TOKEN_REDEEMED`。
  - 权限：学生 token 调 `/api/staff/claim-tokens/redeem` → 403 `STAFF_REQUIRED`；无 token 调 `/api/me/reward` → 401 `AUTH_REQUIRED`。
  - 并发：`Promise.all` 两次核销同一码，恰一个成功（200）一个拒绝（409）。
  - 过期：临时把 `events.end_at` 改为 `now-60s`，取码 → 409 `EVENT_NOT_ACTIVE`、核销 → 409 `CLAIM_TOKEN_EXPIRED`，随后恢复原值。
  - 边界：`deviceId=bad-id` → 400 `INVALID_REQUEST`；`eventId=-5` → 400 `INVALID_REQUEST`。
- 每步打印 ✅/❌ 并附关键数据（计数/码值/状态码）；失败不中断，末尾打印 `总结：通过 X/19 步`。
- 启动准备（`node:sqlite` 直连 database.db）：白名单若无「张三」自动插入一条；清理本脚本 4 个测试设备（MAIN / 两并发学生 / 过期学生，`dev_1770000001_smokeTSA` 等）在 redemptions→claim_tokens→user_badges→booth_view_records 的历史数据以保证可重跑。
- 执行细节：第 9 步（重复取码）在逻辑上提前到第 6 步（核销）之前，否则会被 `CLAIM_TOKEN_REDEEMED` 干扰；报告仍按 1..19 编号。
- 环境变量 `BASE_URL`（默认 `http://localhost:3000`）/ `STAFF_CODE`（默认 `staff2026`）/ `DATABASE_PATH` 可覆盖。
- 实跑验证：临时起 server 后 `node test-smoke.js` → **19/19 全绿**，且重跑结果稳定。

## 6. 加 CORS（前端 5173 → 后端 3000）
- 背景：前端跑在 `http://localhost:5173`、后端 `http://localhost:3000`，浏览器默认阻止跨域；后端需放行。
- 改动：`npm install cors`（`cors ^2.8.6`，已进 package.json/package-lock.json）；`src/app.js` 加 `import cors from 'cors'`，并在 `express.json()` 之前、所有路由之前 `app.use(cors())`。
- 采用 `cors()` **默认 `*` 放行**（内网活动、无敏感跨域诉求），**不收紧白名单**（避免仅允许 5173 的显式配置，保持简单）。
- 验证：
  - CORS 预检：模拟 `Origin: http://localhost:5173` 的 OPTIONS → `204`，`Access-Control-Allow-Origin: *`、`Access-Control-Allow-Methods: GET,HEAD,PUT,PATCH,POST,DELETE`、`Access-Control-Allow-Headers: content-type,authorization`。
  - `node test-smoke.js` → **19/19 全绿**，确认加 CORS 未破坏任何业务流程。

## 7. 前端：浏览埋点验证 & 奖励页（④）+ 二维码
- **③ 浏览摊位埋点**已在前端落地并验证通过：`services/boothView.js` 的 `startViewSession`（3 秒计时 + 去重 + 上报），弹窗/toast 与 localStorage 均正常。
- **新增「奖励」tab（第 4 个）**：
  - 新页 `src/pages/reward.js` + `reward.css`（Class 风格，`mount`/`destroy`）。
  - `src/router.js` 的 `TABS` 加 `{ path:'/reward', mod:rewardPage, index:3 }`；`src/components/tab-bar.js` 的 `this.list` 加 `{ path:'#/reward', text:'奖励' }`（两处须同步）。
- 页面功能：
  - 徽章进度：`GET /api/me/reward` → `badge.uniqueBoothCount / requiredUniqueBooths` 进度条；`badge.unlocked` 为真显示「已解锁」。
  - 领取状态：`claimStatus` → `none`「可领取」按钮 / `active` 出券 / `redeemed`「已核销」/ `expired`「活动已结束」。
  - 倒计时：`getEventEndAt()`，`距结束 X 天 X 小时 X 分`；截止前 **6 小时**即显示「活动已结束」（`end - 6h - now`）。
  - 实时刷新：订阅 `boothView.setOnProgressChanged` → 重拉 `getRewardStatus()`；`destroy()` 里清 interval + 反订阅 + `destroyed` 标志防异步回写已卸载节点（onProgressChanged 只有本页订阅，安全）。
  - 领取：点「可领取」→ `POST /api/me/claim-token` → 更新为 `active` 并出券。
- 二维码：`npm install qrcode`（无 canvas 原生依赖，浏览器入口 `lib/browser.js` 用 `document.createElement('canvas')`，Vite 可正常打包）；`QRCode.toDataURL(token,{ width:200, margin:2 })` 生成 PNG data URL 写入券区 `<img>`，二维码下方保留 `claimToken` 文本备用。二维码内容 = claimToken 明文（非核销链接，扫码得码串）。
- 容错一致性：`getRewardStatus` 失败时 `badge`/`reward` 一并置空，徽章区与领取区同显错误文案，避免「徽章显示旧数据、领取区显示报错」的不一致。
- 另：`frontend/src/main.js` 改为先 `ensureLogin().finally(() => start())` —— 先就绪 token 再启动路由，避免首屏接口（尤其奖励页）401。

## 8. 前端：Staff 角色化第 4 tab + 登录/核销（⑤）+ 收集弹窗阈值
- **角色化第 4 个 tab**：`src/router.js` 把 `/reward` 从 `TABS` 移到 `getTabs()`，按 `localStorage.user_role==='staff'` 决定第 4 项——staff→`/staff/redeem`（核销）、学生→`/reward`（奖励）；`src/components/tab-bar.js` 增加 `updateForRole()` 同步刷新第 4 项文案与路径，路由 render 时 `isTab` 分支调用它以在登录后即时更新。
- **新增 staff 登录页** `src/pages/staff.js`：入口 `#/staff?code=...`，识别码须等于 `STAFF_CODE`（staff2026）且姓名在白名单，成功后写 `user_role='staff'`、`auth_user` 并 `wx.switchTab('#/map')`。
- **新增扫码核销页** `src/pages/staff-redeem.js` + `staff.css`：`html5-qrcode` 扫码自动调 `redeemClaimToken`；仅 staff 可访问，否则「无权访问」1.2s 跳回 `#/map`；成功 3 秒后自动恢复扫码，`destroy()` 停摄像头。
- **员工核销反馈样式**：`.redeem-result` 字号 15→19px；「核销成功」用橙色 `#ff7a00`（非紫 happy path，视觉区分成功）。
- **收集弹窗阈值** `src/services/boothView.js` `deliverSuccess()`：按 `uniqueBoothCount` 与 `badges[].requiredUniqueBooths` 精确区分——`<20` 弹「摊位收集 +1」、`===20` 弹「摊位收集任务已完成，可领取奖励」、`>20`（第 20 次之后）不再弹任何弹窗（解决解锁后重复弹「已完成」）；`onProgressChanged` 仍照常回调。
- **白名单新增 4 人**（`database.db`，被 gitignore **不入库**）：王怡雪 / 何玉成 / 徐瀚翔 / 昝达（原 张三 保留），现共 5 人。测试识别码 `STAFF_CODE=staff2026`。

## 待办 / 未提交
- 本次**前端改动待提交**：角色化第 4 tab（tab-bar.js / router.js）、staff 登录与核销页（staff.js / staff-redeem.js / staff.css）、收集弹窗阈值（boothView.js）、`npm install html5-qrcode`（package.json / package-lock.json）；另 `frontend/计划书.md` 改名为 `frontend/plan.md`。
- **白名单变更不入库**（database.db gitignore）：本次已加王怡雪/何玉成/徐瀚翔/昝达，需在部署/上线环境的库上同样执行 `INSERT OR IGNORE INTO staff_whitelist (name) VALUES (...)`。
- `.env`（含真实 JWT_SECRET）不入库。