---

## 一、后端环境变量（必须改）

### `.env` 文件

| 变量 | 开发环境 | 生产环境 | 为什么 |
|:---|:---|:---|:---|
| `JWT_SECRET` | 随便生成的 | **重新生成一个强随机值** | 开发用的密钥可能泄露过 |
| `STAFF_CODE` | `staff2026` | **改成强随机字符串** | `staff2026` 太容易被猜到 |
| `NODE_ENV` | `development` | **`production`** | 关闭 stack 返回、启用生产优化 |
| `PORT` | `3000` | 按部署环境定 | — |

**生成强随机 `STAFF_CODE`：**

```bash
node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
```

**生成新 `JWT_SECRET`：**

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

## 二、后端代码（必须改）

### 1. CORS 白名单

**开发环境：**

```javascript
app.use(cors());  // 允许所有来源
```

**生产环境：**

```javascript
app.use(cors({
  origin: 'https://your-domain.com',  // 只允许你的前端域名
}));
```

### 2. 错误处理不返回 stack

**你的 `errorHandler` 已经处理了：**

```javascript
const isDev = process.env.NODE_ENV === 'development';
if (isDev) body.error.stack = err.stack;
```

**只要 `NODE_ENV=production`，就不会返回 stack。** ✅ 不用改代码。

### 3. 日志

**开发环境：**

```javascript
console.log(`${new Date().toISOString()} ${req.method} ${req.originalUrl}`);
```

**生产环境：**

- 日志应该写到文件，而不是只输出到终端
- 或者接入日志服务（如 Sentry、Logtail）
- 至少加上日志轮转，避免磁盘写满

**如果你只是临时活动，终端日志也够用，不用改。**

---

## 三、前端代码（必须改）

### 1. API 地址

**开发环境：**

```javascript
export const BASE_URL = 'http://localhost:3000';
```

**生产环境：**

```javascript
export const BASE_URL = 'https://your-domain.com/api';  // 或实际后端地址
```

**或者用环境变量：**

```javascript
export const BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000';
```

**然后用 `.env.production` 配置：**

```
VITE_API_URL=https://your-domain.com
```

### 2. 构建产物

**生产环境不要跑 `npm run dev`，要跑 `npm run build`：**

```bash
cd frontend
npm run build
# 产物在 dist/ 目录
```

**然后把 `dist/` 部署到静态服务器（Nginx、Vercel、Netlify 等）。**

---

## 四、数据库（必须做）

### 1. 备份

**上线前，先备份：**

```bash
cp backend/database.db backend/database.db.backup
```

**上线后，定期备份：**

```bash
# 每天凌晨 3 点备份
0 3 * * * cp /path/to/database.db /path/to/backups/database-$(date +\%Y\%m\%d).db
```

### 2. 迁移

**生产环境的数据库要跑一遍迁移：**

```bash
cd backend
node run-migration.js
```

**确认 `schema_migrations` 表里记录了所有版本。**

### 3. 白名单导入

**工作人员白名单要在上线前导入：**

```bash
sqlite3 backend/database.db "INSERT INTO staff_whitelist (name) VALUES ('张三'), ('李四'), ('王五');"
```

**或者用脚本批量导入。**

---

## 五、部署（必须做）

### 1. 后端部署

| 方式 | 适合 |
|:---|:---|
| **PM2** | 传统服务器，进程守护 |
| **Docker** | 容器化部署 |
| **云函数** | Serverless |
| **Railway / Render** | 快速部署 |

**用 PM2 的示例：**

```bash
npm install -g pm2
cd backend
pm2 start src/server.js --name online-map-try
pm2 save
pm2 startup
```

### 2. 前端部署

**把 `dist/` 目录部署到：**

| 方式 | 适合 |
|:---|:---|
| **Nginx** | 传统服务器 |
| **Vercel** | 前端项目，一键部署 |
| **Netlify** | 同上 |
| **GitHub Pages** | 静态站点 |

### 3. HTTPS

**生产环境必须用 HTTPS。**

| 方式 | 说明 |
|:---|:---|
| **Let's Encrypt** | 免费证书，自动续期 |
| **Cloudflare** | 免费 SSL，套一层 CDN |
| **云服务商证书** | 阿里云、腾讯云等 |

---

## 六、上线前的检查清单

### 后端

```
□ JWT_SECRET 改成新的强随机值
□ STAFF_CODE 改成强随机字符串
□ NODE_ENV=production
□ CORS 白名单收紧
□ 数据库迁移跑完
□ 白名单导入
□ 数据库备份
□ 日志配置（可选）
```

### 前端

```
□ BASE_URL 改成生产地址
□ npm run build 构建成功
□ dist/ 部署到服务器
```

### 部署

```
□ HTTPS 配置
□ 域名解析
□ 后端进程守护（PM2 等）
□ 静态资源缓存策略
```

### 验证

```
□ 生产环境跑一遍 test-smoke.js（改 BASE_URL）
□ 手动走一遍完整流程
□ 检查日志有没有报错
□ 检查 localStorage 写入正常
```

---

## 七、临时活动的特殊考虑

**你的活动是校园活动，只开几天。可以简化一些：**

| 项目 | 是否必须 | 说明 |
|:---|:---:|:---|
| HTTPS | ✅ | 必须，浏览器要求 |
| 进程守护 | ✅ | 后端挂了要自动重启 |
| 数据库备份 | ✅ | 万一数据丢了要恢复 |
| 日志轮转 | 🟡 | 可选，几天不会写满 |
| CORS 收紧 | 🟡 | 可选，活动结束后就关 |
| 监控告警 | 🟡 | 可选，人盯着就行 |

---

## 八、最关键的 5 件事

**如果只能记住 5 件事，记这 5 个：**

| # | 事项 | 为什么 |
|:---:|:---|:---|
| 1 | **改 `JWT_SECRET`** | 开发用的可能泄露 |
| 2 | **改 `STAFF_CODE`** | `staff2026` 太容易被猜 |
| 3 | **`NODE_ENV=production`** | 关闭 stack 返回 |
| 4 | **前端 `BASE_URL` 改生产地址** | 否则前端调不到后端 |
| 5 | **HTTPS** | 浏览器要求 |

---

## 九、你现在该做的

**现在还不用管这些。** 先把第 ③ 步验证完，继续做 ④⑤⑥⑦。

**等所有功能做完，再回来对照这份清单上线。**

**建议：现在把这份清单存到一个文件里，比如 `docs/DEPLOY_CHECKLIST.md`，上线时对照。**

---

## 总结

| 类别 | 必须改的 |
|:---|:---|
| **环境变量** | `JWT_SECRET`、`STAFF_CODE`、`NODE_ENV` |
| **后端** | CORS 白名单 |
| **前端** | `BASE_URL`、`npm run build` |
| **数据库** | 迁移、备份、白名单导入 |
| **部署** | HTTPS、进程守护 |

---